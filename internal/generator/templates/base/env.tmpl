# Environment Variables for {{.ProjectName}} - Production
# This file is empty by default - secrets are set via deployment platform (Fly.io, etc.)
# For local development, use .env.local instead
# For reference, see .env.example

{{- if .HasDynamoDB}}
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
{{- end}}

{{- if .HasPostgres}}
DATABASE_URL=
{{- end}}

{{- if .HasAuth}}
JWT_SECRET=
{{- end}}

{{- if .HasPostHog}}
POSTHOG_API_KEY=
{{- end}}


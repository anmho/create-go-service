# {{.ProjectName}} - Development Configuration
# This file is committed to version control

server:
  port: "8080"
  stage: "development"

database:
{{- if .HasDynamoDB}}
  aws_region: "us-east-1"
  table_name: "{{.ProjectName}}-posts-dev"
  endpoint_url: ""  # Uses default AWS SDK configuration (IAM roles when running on AWS infrastructure)
{{- end}}
{{- if .HasPostgres}}
  url: ""  # Set via DATABASE_URL environment variable
{{- end}}

{{- if .HasAuth}}
auth:
  token_expiry: "24h"
{{- end}}

{{- if .HasMetrics}}
metrics:
  enabled: true
  path: "/metrics"
{{- end}}

{{- if .HasPostHog}}
posthog:
  enabled: true
{{- if .PostHogHost}}
  host: "{{.PostHogHost}}"
{{- else}}
  host: "https://app.posthog.com"
{{- end}}
{{- end}}

